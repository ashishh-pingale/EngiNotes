document.addEventListener('DOMContentLoaded',()=>{const body=document.getElementById('pendingNotesBody');const empty=document.getElementById('emptyState');const sample=[{title:'DBMS Notes',subject:'DBMS',branch:'CSE',year:'2',sem:'4',user:'Ash',date:'Today'}];function render(){body.innerHTML='';if(sample.length===0){empty.style.display='block';return;}empty.style.display='none';sample.forEach(n=>{body.innerHTML+=`<tr><td>📄</td><td>${n.title}</td><td>${n.subject}</td><td>${n.branch}</td><td>${n.year}</td><td>${n.sem}</td><td>${n.user}</td><td>${n.date}</td><td><button class='view'>👁</button><button class='approve'>✔</button><button class='reject'>✖</button></td></tr>`});document.getElementById('pendingCount').textContent=sample.length;}render();/* TODO: Replace sample array with Firestore data. */});